<h2>My task manager :</h2>

<ol>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %> - <%= task.details %> - <%= task.completed %> - <%= link_to "🗑",
      task_path(task), data: { turbo_method: :delete, turbo_confirm: "Do you want to delete this task?" } %>
    </li>
  <% end %>
</ol>

<%= link_to "Create new task", new_task_path %>
